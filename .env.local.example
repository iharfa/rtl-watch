# Copy to .env.local for local development against Neon.
# On Vercel the Neon integration injects DATABASE_URL automatically.
# Without it, the app still works fully offline — cloud sync just reports off.
DATABASE_URL=postgres://USER:PASSWORD@YOUR-PROJECT.neon.tech/neondb?sslmode=require
